// Se usa cuando corres "ng build --configuration production" (lo que hace Render)
// IMPORTANTE: reemplaza esta URL por la URL real de tu backend en Render
// una vez que lo despliegues (paso 4 de la guía).
export const environment = {
  production: true,
  apiUrl: 'https://TU-BACKEND.onrender.com/api'
};
